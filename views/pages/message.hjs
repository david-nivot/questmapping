<!DOCTYPE html>
<html>
<head>
    {{> head }}
</head>
<body>
    {{> header }}
    <div class="content">
        <div class="container">
            <div class="row align-middle message-bloc text-center">
                <h3 class="{{class}}">{{message}}</h3>
            </div>

            <div class="row align-middle">
                <div class="col-sm">
                    {{#target}}
                    <a href="{{target}}" class="btn btn-secondary btn-lg btn-block">
                        Retour à la carte
                    </a>
                    {{/target}}
                    {{^target}}
                    <a href="{{frontUrl}}" class="btn btn-secondary btn-lg btn-block">
                        Retour à la carte
                    </a>
                    {{/target}}
                </div>
            </div>
        </div>
    </div>
    {{> footer }}
</body>
</html>
